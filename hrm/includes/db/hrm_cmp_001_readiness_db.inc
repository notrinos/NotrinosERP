<?php
/** HRM-CMP-001 migration readiness and immutable predecessor custody. */
include_once(dirname(__FILE__).'/migration_readiness_db.inc');
include_once(dirname(__FILE__).'/hrm_cmp_001_schema_db.inc');
function hrm_cmp_001_require_source_tokens($patch,$relative,array $tokens,$label){global $path_to_root;$s=@file_get_contents($path_to_root.'/'.$relative);if(!is_string($s))return hrm_migration_fail($patch,HRM_MIGRATION_TAMPER_SUSPECT,$label.' source is missing.');foreach($tokens as $t)if(strpos($s,$t)===false)return hrm_migration_fail($patch,HRM_MIGRATION_TAMPER_SUSPECT,$label.' source/custody contract is incomplete.');return true;}
function hrm_cmp_001_require_predecessor($patch){global $path_to_root;$file=$path_to_root.'/tests/fixtures/payroll/legacy_v1/manifest_v1.0.438.json';$expected='11d36928b8ebb526cdc3d685880e4ac17d4f6394f8496cc209a0c75be9610354';if(!is_file($file)||!is_string($h=@hash_file('sha256',$file))||!hash_equals($expected,$h))return hrm_migration_fail($patch,HRM_MIGRATION_TAMPER_SUSPECT,'HRM-CMP-001 immutable HRM-LVE-001 accepted custody source/custody contract is incomplete.');$m=json_decode((string)@file_get_contents($file),true);$e=is_array($m)?(isset($m['hrm_lve_001_evidence'])?$m['hrm_lve_001_evidence']:null):null;if(!is_array($e)||!isset($m['contract']['version'])||$m['contract']['version']!=='1.0.438'||!isset($e['status'])||$e['status']!=='CLOSED / LOCAL ACCEPTANCE PASS'||!isset($e['database_version'])||$e['database_version']!=='1.0.714'||!isset($e['mandatory_local_gate_status'])||$e['mandatory_local_gate_status']!=='PASS'||!isset($e['next_dependency_after_acceptance'])||$e['next_dependency_after_acceptance']!=='HRM-CMP-001')return hrm_migration_fail($patch,HRM_MIGRATION_TAMPER_SUSPECT,'HRM-CMP-001 semantic HRM-LVE-001 predecessor contract is incomplete.');return true;}
function hrm_cmp_001_require_full_schema($patch,$triggers=true){return hrm_cmp_001_require_schema($patch,$triggers);}
?>
