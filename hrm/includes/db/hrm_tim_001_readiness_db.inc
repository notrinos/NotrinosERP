<?php
/** Fail-closed readiness checks for HRM-TIM-001 migration edges. */
include_once(dirname(__FILE__).'/migration_readiness_db.inc');
include_once(dirname(__FILE__).'/hrm_tim_001_schema_db.inc');
function hrm_tim_001_require_source_tokens($patch,$relative,array $tokens,$label){global $path_to_root;$src=@file_get_contents($path_to_root.'/'.$relative);if(!is_string($src))return hrm_migration_fail($patch,HRM_MIGRATION_TAMPER_SUSPECT,$label.' source is missing.');foreach($tokens as $token)if(strpos($src,$token)===false)return hrm_migration_fail($patch,HRM_MIGRATION_TAMPER_SUSPECT,$label.' source/custody contract is incomplete.');return true;}
function hrm_tim_001_require_predecessor($patch){global $path_to_root;$relative='tests/fixtures/payroll/legacy_v1/manifest_v1.0.432.json';$file=$path_to_root.'/'.$relative;$expected='9fdf4c843b50346a5e6ef4815c81e3cf64ce18a760b8ccb63bfdf0a0d6f1cac2';if(!is_file($file)||!is_string($hash=@hash_file('sha256',$file))||!hash_equals($expected,$hash))return hrm_migration_fail($patch,HRM_MIGRATION_TAMPER_SUSPECT,'HRM-TIM-001 immutable PAY-CORE-010 successor-pointer custody source/custody contract is incomplete.');$m=json_decode((string)@file_get_contents($file),true);$e=is_array($m)?($m['pay_core_010_evidence']??null):null;if(!is_array($e)||($m['contract']['version']??null)!=='1.0.432'||($e['status']??null)!=='CLOSED / LOCAL ACCEPTANCE PASS'||($e['database_version']??null)!=='1.0.687'||($e['mandatory_local_gate_status']??null)!=='PASS'||($e['next_dependency']??null)!=='HRM-TIM-001')return hrm_migration_fail($patch,HRM_MIGRATION_TAMPER_SUSPECT,'HRM-TIM-001 immutable PAY-CORE-010 semantic predecessor contract is incomplete.');return true;}
?>
