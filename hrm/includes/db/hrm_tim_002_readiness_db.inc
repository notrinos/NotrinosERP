<?php
/** Fail-closed source/predecessor readiness for HRM-TIM-002. */
include_once(dirname(__FILE__).'/migration_readiness_db.inc');
include_once(dirname(__FILE__).'/hrm_tim_002_schema_db.inc');
function hrm_tim_002_require_source_tokens($patch,$relative,array $tokens,$label){global $path_to_root;$s=@file_get_contents($path_to_root.'/'.$relative);if(!is_string($s))return hrm_migration_fail($patch,HRM_MIGRATION_TAMPER_SUSPECT,$label.' source is missing.');foreach($tokens as $t)if(strpos($s,$t)===false)return hrm_migration_fail($patch,HRM_MIGRATION_TAMPER_SUSPECT,$label.' source/custody contract is incomplete.');return true;}
function hrm_tim_002_require_predecessor($patch){global $path_to_root;$file=$path_to_root.'/tests/fixtures/payroll/legacy_v1/manifest_v1.0.434.json';$expected='05e1ea6533ecc04bca9a493f805cccda23566b3c629c260bf40179426535340e';if(!is_file($file)||!is_string($h=@hash_file('sha256',$file))||!hash_equals($expected,$h))return hrm_migration_fail($patch,HRM_MIGRATION_TAMPER_SUSPECT,'HRM-TIM-002 immutable HRM-TIM-001 accepted custody source/custody contract is incomplete.');$m=json_decode((string)@file_get_contents($file),true);$e=is_array($m)?($m['hrm_tim_001_evidence']??null):null;$r=is_array($m)?($m['pay_rule_005_evidence']??null):null;if(!is_array($e)||($m['contract']['version']??null)!=='1.0.434'||($e['status']??null)!=='CLOSED / LOCAL ACCEPTANCE PASS'||($e['database_version']??null)!=='1.0.695'||($e['mandatory_local_gate_status']??null)!=='PASS'||($e['next_dependency_after_acceptance']??null)!=='HRM-TIM-002')return hrm_migration_fail($patch,HRM_MIGRATION_TAMPER_SUSPECT,'HRM-TIM-002 semantic HRM-TIM-001 predecessor contract is incomplete.');if(!is_array($r)||($r['local_acceptance_status']??null)!=='PASS'||($r['closure_status']??null)!=='PAY-RULE-005 COMPLETE / LOCAL ACCEPTANCE PASS')return hrm_migration_fail($patch,HRM_MIGRATION_TAMPER_SUSPECT,'HRM-TIM-002 PAY-RULE-005 accepted policy predecessor contract is incomplete.');return true;}
function hrm_tim_002_require_full_schema($patch,$triggers=true){return hrm_tim_002_require_schema($patch,$triggers);}
?>
