<?php
/** HRM-BEN-001 migration readiness and immutable accepted CMP predecessor custody. */
include_once(dirname(__FILE__).'/migration_readiness_db.inc');
include_once(dirname(__FILE__).'/hrm_ben_001_schema_db.inc');
function hrm_ben_001_require_source_tokens($patch,$relative,array $tokens,$label){global $path_to_root;$s=@file_get_contents($path_to_root.'/'.$relative);if(!is_string($s))return hrm_migration_fail($patch,HRM_MIGRATION_TAMPER_SUSPECT,$label.' source is missing.');foreach($tokens as $t)if(strpos($s,$t)===false)return hrm_migration_fail($patch,HRM_MIGRATION_TAMPER_SUSPECT,$label.' source/custody contract is incomplete.');return true;}
function hrm_ben_001_require_predecessor($patch){global $path_to_root;$file=$path_to_root.'/tests/fixtures/payroll/legacy_v1/manifest_v1.0.440.json';$expected='4535f5e22f037d734ec87b9783245233f9e3f4ff320b9ea66cf47d5f26436e70';if(!is_file($file)||!is_string($h=@hash_file('sha256',$file))||!hash_equals($expected,$h))return hrm_migration_fail($patch,HRM_MIGRATION_TAMPER_SUSPECT,'HRM-BEN-001 immutable HRM-CMP-001 accepted custody source/custody contract is incomplete.');$m=json_decode((string)@file_get_contents($file),true);$e=is_array($m)?($m['hrm_cmp_001_evidence']??null):null;if(!is_array($e)||($m['contract']['version']??'')!=='1.0.440'||($e['status']??'')!=='CLOSED / LOCAL ACCEPTANCE PASS'||($e['database_version']??'')!=='1.0.725'||($e['mandatory_local_gate_status']??'')!=='PASS'||($e['next_dependency_after_acceptance']??'')!=='HRM-BEN-001'||($e['hrm_ben_001_started']??true)!==false)return hrm_migration_fail($patch,HRM_MIGRATION_TAMPER_SUSPECT,'HRM-BEN-001 semantic HRM-CMP-001 predecessor contract is incomplete.');return true;}
function hrm_ben_001_require_full_schema($patch,$triggers=true){return hrm_ben_001_require_schema($patch,$triggers);}
?>
