<?php
/** Fail-closed readiness checks shared by PAY-CORE-010 migration edges. */
include_once(dirname(__FILE__).'/migration_readiness_db.inc');
include_once(dirname(__FILE__).'/pay_core_010_schema_db.inc');
function hrm_pay_core_010_require_source_tokens($patch,$relative,array $tokens,$label){global $path_to_root;$src=@file_get_contents($path_to_root.'/'.$relative);if(!is_string($src))return hrm_migration_fail($patch,HRM_MIGRATION_TAMPER_SUSPECT,$label.' source is missing.');foreach($tokens as $token)if(strpos($src,$token)===false)return hrm_migration_fail($patch,HRM_MIGRATION_TAMPER_SUSPECT,$label.' source/custody contract is incomplete.');return true;}
function hrm_pay_core_010_require_predecessor($patch){global $path_to_root;$relative='tests/fixtures/payroll/legacy_v1/manifest_v1.0.429.json';$file=$path_to_root.'/'.$relative;$expected='60e5fd8dbd27c69d77d2208569b23946deca2b8ed756dd8cd70ef508f96c4096';if(!is_file($file)||!is_string($hash=@hash_file('sha256',$file))||!hash_equals($expected,$hash))return hrm_migration_fail($patch,HRM_MIGRATION_TAMPER_SUSPECT,'PAY-CORE-010 immutable PAY-CORE-009 predecessor custody source/custody contract is incomplete.');$m=json_decode((string)@file_get_contents($file),true);$e=is_array($m)?($m['pay_core_009_evidence']??null):null;if(!is_array($e)||($m['contract']['version']??null)!=='1.0.429'||($e['status']??null)!=='CLOSED / LOCAL ACCEPTANCE PASS'||($e['database_version']??null)!=='1.0.669'||($e['mandatory_local_gate_status']??null)!=='PASS'||($e['final_operational_closed']??null)!==true||($e['next_dependency']??null)!=='PAY-CORE-010')return hrm_migration_fail($patch,HRM_MIGRATION_TAMPER_SUSPECT,'PAY-CORE-010 immutable PAY-CORE-009 predecessor semantic contract is incomplete.');return true;}
function hrm_pay_core_010_require_approval_schema($patch){return hrm_pay_core_010_require_group_schema($patch,'approval',false);}
function hrm_pay_core_010_require_projection_schema($patch){return hrm_pay_core_010_require_approval_schema($patch)&&hrm_pay_core_010_require_group_schema($patch,'projection',false);}
function hrm_pay_core_010_require_cutover_schema($patch){return hrm_pay_core_010_require_projection_schema($patch)&&hrm_pay_core_010_require_group_schema($patch,'cutover',false);}
?>
