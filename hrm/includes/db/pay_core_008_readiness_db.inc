<?php
/** Fail-closed source/readiness checks shared by PAY-CORE-008 migration edges. */
include_once(dirname(__FILE__).'/migration_readiness_db.inc');
function hrm_pay_core_008_require_source_tokens($patch,$relative,array $tokens,$label){global $path_to_root;$s=@file_get_contents($path_to_root.'/'.$relative);if(!is_string($s))return hrm_migration_fail($patch,HRM_MIGRATION_TAMPER_SUSPECT,$label.' source is missing.');foreach($tokens as $t)if(strpos($s,$t)===false)return hrm_migration_fail($patch,HRM_MIGRATION_TAMPER_SUSPECT,$label.' source/custody contract is incomplete.');return true;}
function hrm_pay_core_008_require_predecessor($patch){global $path_to_root;$relative='tests/fixtures/payroll/legacy_v1/manifest_v1.0.425.json';$p=$path_to_root.'/'.$relative;$expected='f24e3a9cd778883fbc7fde99ab972f3b767b84ddf99d5498e96604ed1da061b0';if(!is_file($p)||!is_string($h=@hash_file('sha256',$p))||!hash_equals($expected,$h))return hrm_migration_fail($patch,HRM_MIGRATION_TAMPER_SUSPECT,'PAY-CORE-008 immutable predecessor custody source/custody contract is incomplete.');$m=json_decode((string)@file_get_contents($p),true);if(!is_array($m)||($m['contract']['version']??null)!=='1.0.425'||($m['pay_core_007_evidence']['status']??null)!=='CLOSED / LOCAL ACCEPTANCE PASS'||($m['pay_core_007_evidence']['mandatory_local_gate']['status']??null)!=='PASS'||($m['pay_core_007_evidence']['final_operational_closed']??null)!==true)return hrm_migration_fail($patch,HRM_MIGRATION_TAMPER_SUSPECT,'PAY-CORE-008 immutable predecessor custody semantic contract is incomplete.');return hrm_pay_core_008_require_source_tokens($patch,'hrm/includes/payroll_engine.inc',array('return calculate_employee_payslip_legacy_v1($employee, $from_date, $to_date, $period_id, $context);'),'PAY-CORE-008 legacy production authority');}
?>
